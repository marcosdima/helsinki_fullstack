{
  "name": "agenda_backend",
  "version": "1.0.0",
  "description": "Exercise for Helsinki",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && cd ../agenda/ && npm run build && cp -r dist ../agenda_backend",
    "start": "node ./src/index.js",
    "dev": "nodemon  ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  }
}
